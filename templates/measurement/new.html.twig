{# templates/measurement/new.html.twig #}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create new Measurement</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-success">Create</button>
    {{ form_end(form) }}

    <a href="{{ path('app_measurement_index') }}">Back to list</a>
{% endblock %}
